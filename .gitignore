# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust build artifacts
src-tauri/target
src-tauri/gen/schemas

# Vendored agent skills (one-off audit tooling — reinstall with
# `npx skills add nutlope/hallmark`). Same reasoning as ADR 0012: a lean repo
# doesn't carry 67k of third-party tooling that one command restores.
.agents/
.claude/skills/
skills-lock.json

# Scratch driver output
.playwright-cli/

# Agent worktrees + local session settings — machine-local, never shared
.claude/worktrees/
.claude/settings.json

# cmux cockpit: machine-local config, scripts and scratch, never shared
.cmux/

# Audit and scan output — never commit (contains environment dumps)
audit-output/
